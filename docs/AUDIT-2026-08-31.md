# Audit — 2026-08-31

Single closing record for the full docs+code+structure audit run this session. Supersedes the pattern of prior audit rounds (`docs/plans/2026-08-21-reaudit-attack-plan.md`, `-p0-p1-attack-plan.md`, `2026-08-24-consolidated-audit-implementation-plan.md`, `2026-08-25-complete-audit-implementation-plan.md`) that each opened new findings without closing the original P0/P1 backlog. This document exists so the next audit does not repeat that: **it reconciles what was already open before adding anything new.**

Full audit plan: `C:\Users\arifr\.claude\plans\squishy-noodling-robin.md`.

## Reading order (this document has four layers, oldest first)

1. **Findings** (original sections below "Fase 0" through "Fase 5-PRAC-directed") — written *before* any implementation, when this was purely an audit. Some of it is now stale.
2. **Post-audit implementation** (section right below this one) — the first implementation wave, written after landing 9 commits.
3. A `⚠️ SUPERSEDED` banner marks the one section (original "Fase 4") that layer 2 made factually wrong; it's collapsed into a `<details>` block, kept for history only.
4. **Current residual status** (right here, below) — the actual up-to-date answer, after two rounds of external post-implementation review found and fixed a real P0 gap (the recovery pipeline bypassing the new scheduler) plus several smaller issues. **This section is the one to trust for "what's true right now."** `docs/ledger/ledger.json` is the underlying source of truth per item; this is a summary of it.

## Current residual status (authoritative — 2026-08-31, 18 commits in)

**Fixed, tested, verified correct by direct code read:** `TS-AUDIT31-EX-01`, `TS-AUDIT31-EX-02`, `TS-AUDIT31-PX-01` (including `/health` exposure), `TS-REAUDIT-01`, `TS-REAUDIT-04` (gateway + profile), `TS-REAUDIT-11`, `TS-REAUDIT-07`'s size/cycle ratchet, the `/health` schema v2→v3 pairing bump, PR-F/G/H's core implementation including the recovery-pipeline scheduler routing fixed after review.

**Marked `in_progress`, honestly, not `done`:**
- `TS-REAUDIT-08` — the fix is correct; an `AppService`-level end-to-end test proving it (critical disposer fails → stores/lock retained → restart recovers) does not exist. Building one needs a test harness this repo has never had for `AppService`.
- `TS-REAUDIT-10` — CI wiring is real and verified; ENOSPC and SQLite-corruption fault cases remain unattempted (SQLite-busy was added; see below).
- `TS-REAUDIT-06` — supervisor stays observe-only; owner/gate-contract table, shadow soak, and a promotion mechanism don't exist.
- `TS-REAUDIT-05` — all caches are code-bounded; only the multi-day soak proof is missing.
- `TS-STREAM-RECOVERY-01` — PR-F/G/H implemented and reviewed twice; **the 72h PRAC soak is still the only thing that can make this trusted for continuous armed operation, and nothing in this session changes that.**

**Explicitly out of scope for a code-fix session, not a bug:** `release/paired-release.json` and `release/ci-artifact/paired-release.json` are frozen artifacts of the actual last formal release (gateway 0.2.2, 21/08) — they only get regenerated by a human triggering `release.yml`'s `workflow_dispatch` with real PRAC evidence, which nobody did this session. They are correctly stale, not broken.

**Nothing has been pushed to any remote.** All work is local commits on `agent/audit-implementation-2026-08-31` in both `glitch-topstep` and `glitch-topstep-hermes-profile`.

---

**What this audit could not do, and did not fake:** Fase 4's 72h PRAC soak and Fase 5's live PRAC-directed tests require running the gateway against real Topstep/ProjectX infrastructure for days, with credentials this session does not have and should not use unattended. Those are documented below as blocked with an exact procedure, not marked done.

---

## Post-audit implementation (same session, same day)

Everything below this line was **implemented**, not just found, after this document was first written — following `docs/plans/2026-08-31-plano-completo-implementacao-auditoria.md`. 11 commits on branch `agent/audit-implementation-2026-08-31` (gateway) + 1 commit on the same branch name in `glitch-topstep-hermes-profile`, nothing pushed, `npm run check` green (532/532) after every commit. Full evidence for each item is in its `docs/ledger/ledger.json` `closure_note`/`audit_2026_08_31_note` — this is a summary, not a duplicate.

**Fase 1 (P0, gateway + profile):**
- `TS-AUDIT31-EX-01` — real bug fixed: a suspended (undocumented `status:8`) bracket child could be reported as proven protection with `price:null`. Fixed in `protection.ts` and `protection-supervisor.ts`.
- `TS-REAUDIT-08` — shutdown could release the runtime lock and close stores while a critical disposer (e.g. `realtime`) had failed to unwind, potentially still holding a live writer. Fixed with a new pure `requiresShutdownRetention()`.
- `TS-REAUDIT-04` — `/intent/receipt`'s bare 404 could be mistaken for "safe to discard" by the profile's outbox pruning. Fixed on both sides: the gateway route now falls back to the `/intent/status` envelope instead of 404ing a non-terminal intent; the profile's `gateway_receipt_gate()` now cross-checks a 404 against `/intent/status` before ever discarding.

**Fase 2 (isolation, MARKET-only guard, fault matrix in CI):**
- `TS-AUDIT31-EX-02` — regression test pinning entry submission to MARKET-only (guards the REST reconciliation blind spot documented in `PROJECTX-API-REFERENCE.md` D3).
- `TS-AUDIT31-PX-01` — `ReadCircuitBreaker` now isolates failure/cooldown state per endpoint family so a `retrieveBars` failure burst can't trip the breaker for reconciliation-critical reads.
- `TS-REAUDIT-01` — bounded jittered backoff on repeated auth-refresh failure.
- `TS-REAUDIT-10` — the fault-matrix gate (kill-point, lifecycle, auth-backoff proofs) now runs on every push/PR in `ci.yml`, not just the manual release workflow. Verified end to end locally against both real repos before wiring in. ENOSPC/SQLite-busy/SQLite-corruption fault cases explicitly deferred — need new test infrastructure, not rushed.

**Fase 5/6 (observability, architecture):**
- `TS-REAUDIT-11` — health alerts went from a stateless recompute-every-call function to a real evaluation-count-based hysteresis/dedup tracker (`HealthAlertTracker`), persisted for the process lifetime. `docs/OPERATIONS.md`'s SLO table corrected to state what's actually implemented (tick-based, not wall-clock-second-based as it previously implied) versus what's still aspirational (`evidence_queue_depth` numeric thresholds, `auth_degraded`'s compound condition).
- `TS-REAUDIT-07` — `service.ts`/`coordinator.ts` had grown across three "extraction" waves instead of shrinking, with no numeric baseline to be falsifiable against. Added a real baseline + a CI-enforced size ratchet (growth is still possible, just not silent — bump the baseline deliberately in the same commit) plus a first-party import-cycle detector, which found **4 genuine pre-existing cycles** (not introduced this session), now recorded in an explicit allowlist rather than fixed blind.

**Fase 3 — PR-F/G/H (implemented at the user's explicit request despite the size/risk; explicitly UNSOAKED):**
- **PR-F**: new `TaskScheduler` coordinates the four previously fully-independent timers (reconcile, market observation, order flow, history sync) — same cadences as before, but now with bounded concurrency, explicit priority order, duplicate-request coalescing, and a starvation guard, instead of four uncoordinated fire-and-forgets that could all pile up post-reconnect. Fully unit-tested in isolation (7 tests).
- **PR-G**: found the "cheap vs full reconcile cycle" ask was already implemented before this session. Added the one real gap — bounded jittered backoff on repeated reconcile failure. Did not implement "higher frequency during recovery" — judged too invasive to the core reconcile timing for this pass.
- **PR-H**: added `persistence_bytes` (per-store SQLite file size), `heap_used_bytes`, and `health_build_ms` to `/health`. Did not instrument SQLite-internal latency/busy-lock/fsync — a materially larger, more invasive change than this pass's risk budget.
- **`TS-STREAM-RECOVERY-01` stays `in_progress`.** None of PR-F/G/H should be treated as trusted for continuous armed operation until the 72h PRAC soak this ticket's own acceptance criteria already require actually runs — that has not changed.

**Ledger tally:** started this session with 10 `open` P0/P1 items; ends with `TS-REAUDIT-05` (blocked purely on the soak deliverable, code already fully bounded) and `TS-REAUDIT-06` (partially done — persisted `unprotected_since_utc` and supervisor/gate divergence alerting are wired; owner/gate-contract table, shadow soak, and an actual promotion mechanism remain, deliberately not rushed) as the only two still open. Three new findings (`TS-AUDIT31-EX-01/EX-02/PX-01`) were surfaced and two of three closed with code fixes.

**What still needs your decision** (unchanged from before this section, repeated for visibility): the 7 git worktrees under `Fase 0` — 3 safely removable (fully merged), 4 unmerged and not touched.

---

## Fase 0 — Backlog reconciliation

### 0.1 — Corrected this session's own error

`ARCHITECTURE.md` briefly claimed (added earlier this session) that the absence of bracket parent/child evidence was "a structural limit of the documented Gateway API." That was wrong on first correction, and the *first correction* was also wrong — both times because I trusted a secondary source (`docs/PROJECTX-API-REFERENCE.md`, dated 31/jul) instead of reading the current code directly. Verified by direct code read: `src/projectx/schemas.ts:172-177` captures `parentOrderId`/`linkedOrderId`, and `src/ownership/protection.ts` (`resolveProtectiveLeg`) already uses `parentOrderId` as a fallback ownership match. `ARCHITECTURE.md` and `PROJECTX-API-REFERENCE.md` §D1 are now corrected to match the actual code. **Lesson applied going forward:** a doc that grades its own claims by evidence date (`[LIVE]` etc.) is still a snapshot — re-verify against current code before citing it, not just before writing new prose from it.

### 0.2 — Disposition of the 10 `open` P0/P1 items

| ID | Before | After | Why |
|---|---|---|---|
| TS-REAUDIT-01 | open P0 | **open P1**, scoped note | REST facade consolidation is done (single `ProjectXAuthManager.authenticatedClient()` path, no bypass). Only the jittered-backoff-on-refresh-failure sub-item remains. |
| TS-REAUDIT-02 | open P0 | **done** | Durable evidence outbox exists and is exercised at startup (`sqlite-provider-evidence-store.ts`, `service.ts:238-251`); durable backlog blocks clean shutdown. |
| TS-REAUDIT-03 | open P0 | **done** | Flatten fallback now requires `ownWorkingOrders===0` (`flatten-control-saga.ts:36-40`), regression-tested. |
| TS-REAUDIT-04 | open P0 | open, scoped note | Gateway-side `/intent/status` never 404s; the legacy `/intent/receipt` route still does, and the crash-after-registration path is unit- but not integration-tested. |
| TS-REAUDIT-05 | open P1 | open, scoped note | All three named caches are now bounded (rearm clears on flat, trade-outcome LRU 2048, REST hash-cache LRU 4096 with `/health` eviction metrics). Only the multi-day heap-slope soak proof is missing — same class of gap as the project-wide soak problem below. |
| TS-REAUDIT-06 | open P1 | open, scoped note | Safety supervisor is confirmed still observe-only by design (`evaluateSafetySupervisor` always returns `mode: "observe"`); two of four original resolution items are wired, two are not. |
| TS-REAUDIT-07 | open P1 | open, scoped note | Measured for the first time: `coordinator.ts` 1596→1612 lines, `service.ts` 1420→1588 lines since the 08-21 baseline, despite three extraction waves. Extraction is real, but new orchestration logic lands as fast as old logic is extracted. |
| TS-REAUDIT-08 | open P0 | open, scoped note | `stopSerial`'s retain-state check only looks at evidence-queue/outbox backlog, not at whether a failed critical disposer still has a live writer. Real gap, no test at the `AppService` level yet. |
| TS-REAUDIT-10 | open P0 | **open P1** (downgraded) | No longer "a file-list stub": `reaudit-fault-matrix-gate.mjs` runs real fault-injection tests and gates the armed-release GitHub Actions workflow. Real gaps: not wired into `npm run check`/CI, and no ENOSPC/corruption/stream-lag cases in the proof bundle. |
| TS-REAUDIT-11 | open P1 | open, scoped note | Confirmed: `health-alerts.ts` is a pure stateless recompute-every-call function — no hysteresis, no dedup, no runbook link — while `OPERATIONS.md` documents a "Hysteresis" column that doesn't exist in code. |

**Net: 2 of 10 closed with evidence (02, 03), 1 downgraded (10), 7 kept open with a precisely scoped remaining-work note instead of the vague original finding.** None were silently carried forward unexamined — the failure mode this audit exists to stop.

`TS-STREAM-RECOVERY-01` stays `in_progress` — the 2026-08-31 flapping recurrence (61 restarts) is now recorded in its own note as confirmation that the 72h soak gate was correctly withheld. *(At the time this section was first written, PR-F/G/H had not been started — that has since changed; see "Current residual status" at the top of this document for what's true now.)*

### 0.3 — Three new P0/P1 findings (not previously ticketed)

Added to the ledger as `TS-AUDIT31-EX-01`, `TS-AUDIT31-EX-02`, `TS-AUDIT31-PX-01`:

- **`TS-AUDIT31-EX-01` (P0, real bug, confirmed by code read):** ProjectX's undocumented `status: 8` (a bracket child suspended between placement and entry fill, price fields null) is not distinguished anywhere from a fully-working leg. `resolveProtectiveLeg`/`evaluateProtectionHealth` can report a `status:8` leg as **proven protection with `price: null`**. Reachable today via the scale-in path (a new bracketed entry while already positioned). Currently masked because entries are market-only (~8ms window).
- **`TS-AUDIT31-EX-02` (P1, confirmed latent):** REST reconciliation (`Order/searchOpen`) never returns `status: 8` rows — a freshly-allocated bracket is genuinely invisible to REST recovery in that window. Bounded to ~8ms only because entries are market-only; would become a real exposure window on any future move to LIMIT/STOP entries.
- **`TS-AUDIT31-PX-01` (P1, design finding):** `ReadCircuitBreaker` shares one failure counter across every read endpoint. A burst of failures on one endpoint (e.g. `retrieveBars` during reconnect) can trip the breaker for unrelated reconciliation reads too — directly relevant to the 24–31/08 flapping.

These three, plus everything below, are the highest-value output of this audit: two of them are real, previously-unticketed safety gaps in the exact subsystem (`ownership`/`execution`) that carries live-money risk.

---

## Fase 1 — Documentation consistency

Fixed this session (all edits are in place, not just recommended):

| Doc | Fix |
|---|---|
| `README.md` | Endpoint table now matches the actual live routes (`src/server/local-gateway.ts`) — added `/outcomes`, `/outcomes/feed`, `/execution/facts`, `/intent/status`, `/intent/receipt`, `/control`; noted `GATEWAY-SPEC.md`'s `/intents` (plural) is a rebuild-target name, not the live route. Removed the hardcoded "Suggested next work" ledger snapshot (was already stale) in favor of a pointer to `docs/ledger/ledger.json`. |
| `GATEWAY-SPEC.md` | Footnoted the `/intents` vs `/intent` naming gap. Corrected the `/health` `safety_supervisor` line to state it's currently observe-only, not "aligned with execution gates" without qualification. |
| `TOPSTEP-NATIVE.md` | Principle 6 corrected: `MOVE_STOP`/`MOVE_TP` are live-validated, not disabled. "Last reviewed" updated to this audit's date. |
| `ROADMAP.md` | R4's `MOVE_STOP`/`MOVE_TP` checkboxes corrected to `[x]` with a caveat (ownership proof strength, not the mutation itself, is what's still open). Added a header note establishing `PARITY.md` as the tie-breaker when Roadmap and Parity disagree. |
| `THREAT-MODEL.md` | "Duplicate execution" and "Stale state" required-mitigation lists updated — most were already implemented and the doc hadn't caught up (cross-checked against `ROADMAP.md` R0/R1). "Unprotected fills" rewritten to reference the precise D1/D2/D3 state instead of a generic "required before acceptance" list. |
| `AUTHORITY.md` ↔ `docs/specs/TS-AUDIT-03.md` | Cross-linked instead of duplicating the same decision text in both places. |
| `ARCHITECTURE.md` | Linked `PROJECTX-API-REFERENCE.md` from the top-level "related docs" line — it's the most evidence-rigorous doc in the repo and wasn't cited by either canonical doc. Bracket-ownership section corrected twice this session (see 0.1) to match verified current code. |
| `docs/PROJECTX-API-REFERENCE.md` | §D1 marked resolved with an explicit before/after, plus a one-line lesson about re-verifying dated evidence against current code. |

Deferred (lower value for the time cost, logged here rather than silently dropped): `docs/specs/*.md` still lack a consistent `status:` convention; `HANDOFF.md` is correctly self-labeled historical but still sits at the top level of `docs/` instead of an archive folder — cosmetic, not confusing anyone today.

---

## Fase 2 — Code audit by subsystem

Full per-file findings are in the three completed investigation passes; summarized here by subsystem. Every item below either has a ledger ID (`TS-AUDIT31-*` or an existing `TS-REAUDIT-*`) or is explicitly marked informational/no-action.

**`src/projectx/` + `src/storage/`** (highest "ponytail" density, 10 markers):
- `TS-AUDIT31-PX-01` (shared circuit-breaker bucket) — new, above.
- `rest-concurrency.ts`: still a flat 4-slot semaphore, no priority; this global REST gate is separate from PR-F's later `TaskScheduler` (which coordinates dispatch order/concurrency of the periodic and recovery-triggered work, not the raw REST concurrency floor) — tracked under `TS-STREAM-RECOVERY-01`. *(At the time this was written, PR-F itself had not been started — it was implemented later this session; see "Current residual status" at the top of this document.)*
- `client.ts:155` (`captureAuthEnvelopes`, fixture-only method that bypasses `ProjectXAuthManager` bookkeeping if ever called live) — flagged, not fixed; recommend guarding it test/script-only.
- `evidence-write-queue.ts` `restore()` doesn't restore the coalescing index for quote/depth entries after a failed batch write (identity entries are unaffected) — minor, noted for a future pass.
- D5 (`updateTimestamp` required as non-null string) — **fixed**: now falls back to `creationTimestamp` instead of throwing, matching an existing pattern elsewhere in the codebase. `npm run check` green after the change.
- Everything else in this file set (client.ts:34, schemas.ts:146, realtime.ts:494/606) — read and confirmed informational/already-mitigated, no action needed.

**`src/execution/` + `src/ownership/` + `src/state/`** (second-highest density, `coordinator.ts` alone has 6 markers):
- `TS-AUDIT31-EX-01`, `TS-AUDIT31-EX-02` — new, above, the two highest-value findings of the whole audit.
- `coordinator.ts:1321` — `AUTO_BREAKEVEN` stop-tighten amendments bypass the durable outbox/mutation-state record; naturally idempotent (next pass re-checks live venue price before resubmitting) so it's an audit-trail gap, not a duplicate-mutation risk. Logged here, no ledger ID — doesn't meet the bar for a standalone P-something item.
- `coordinator.ts:375,617` — silent catch around a mutation-provider-orderId note; traced fully, confirmed benign (the value is captured immediately after regardless).
- `venue-state.ts`/`working-orders.ts` terminal-state sets (`[2,3,4,5]`) — confirmed to agree with each other, no drift.

**`src/safety/` + `src/observability/` + `src/market/`**:
- `safety-supervisor.ts` — confirmed observe-only by design; `GATEWAY-SPEC.md` corrected (Fase 1) to say so plainly.
- `data-alignment-phase-d-gate.ts` — confirmed orphaned: gated behind two env vars neither set anywhere, and called from no production path. Dead code, not an active risk. No ledger ID — recommend deleting in a future pass once confirmed genuinely unused by anything planned.
- `atomic-file.ts`'s Windows fsync-skip workaround — confirmed still necessary (Windows-only deployment per `OPERATIONS.md`; Windows genuinely can't open a directory handle for fsync). No action.
- The four independent timers (`reconcile` 3s, `history sync` 60s, `market observation` 60s, `order flow` 10s in `src/service.ts`) — confirmed still uncoordinated at the time this was written, corroborating why PR-F mattered. *(PR-F was implemented later this session: the timers still exist and still own cadence, but the REST-bound work they trigger is now dispatched through a coordinated `TaskScheduler` — see "Current residual status" at the top of this document.)* Cross-referenced into `TS-STREAM-RECOVERY-01`.

All code changes made during this audit (D5 null-tolerance fix, no others) passed `npm run build` and the full `npm test` suite (492/492 green) before being kept.

---

## Fase 3 — Repository structure

**Deleted (confirmed safe — not git-tracked, not referenced anywhere, not a registered worktree):**
- 6 `.kill-matrix-data*` directories (stale `gateway.stderr.log`/`gateway.stdout.log` from past acceptance runs)
- `hermes-profile/` (confirmed empty at every level, zero references in code or docs)
- the stray empty `=` file
- `.pr-body-gateway.md` (a PR description draft for an already-merged 2026-08-25 wave — content is in git history via the actual merged PR)
- `scripts/_monitor_phase_c.py` (one-off debug script, hardcoded a path on a different machine, zero references anywhere in the repo)

**NOT deleted — this needs your decision, not mine:**

`git worktree list` revealed that 7 of the "cruft" directories the plan assumed were dead are actually **registered git worktrees**, each checked out to its own branch:

| Directory | Branch | Merged into `main`? |
|---|---|---|
| `.acceptance-foundation` | `agent/acceptance-foundation` | Yes (0 commits ahead) — has 3 uncommitted fixture-JSON modifications |
| `.hermes-foundation` | `agent/hermes-outcome-store` | Yes (0 commits ahead) |
| `.profile-foundation` | `agent/profile-outcome-store` | Yes (0 commits ahead) |
| `.fix-pr129` | `codex/fix-pr129` | **No — 1 commit ahead of main, unmerged** |
| `.fix-pr130` | `codex/fix-pr130` | **No — 2 commits ahead, unmerged** |
| `.implementation-work` | `codex/implement-ledger-1-27` | **No — 3 commits ahead, unmerged** |
| `.prod-05-evidence` | `feat/prod-05-evidence-backpressure` | **No — 1 commit ahead, unmerged** |

The three fully-merged ones (`acceptance-foundation`, `hermes-foundation`, `profile-foundation`) are safe to remove via `git worktree remove` — their content already lives in `main`. The four unmerged ones might contain real work that never landed, or might be abandoned experiments — I can't tell which without reading each branch's diff, and deleting a worktree with unmerged commits is not something to guess on for a live-money-adjacent repo.

Also found and **not touched** (out of this audit's scope — they belong to the sibling `glitch-topstep-hermes-profile` repo, not this one): `.hermes-foundation2` and `.profile-implementation` are worktrees registered in that *other* repo's git, nested inside this repo's directory by path. `.merge-hermes-work` is a full independent git clone, not a worktree — also untouched pending your call.

`scripts/` triage: contrary to the plan's assumption, almost everything there is load-bearing. `_clear-r1-04-test-intents.mjs` is cited as evidence in a `done` ledger item — kept. `pm4-phase-c-e2e.mjs` and `reaudit-fault-matrix-proofs.json` are referenced by `docs/PARITY.md` and the armed-release CI workflow respectively — kept. Only one script (`_monitor_phase_c.py`) was genuinely orphaned.

---

## Fase 4 — Reliability (stream-recovery closure)

**⚠️ SUPERSEDED.** Everything below this line was true when first written (before implementation began) and is kept only as a historical record of the decision at that point. **PR-F/G/H were subsequently implemented in this same session, at the user's explicit request** — see "Post-audit implementation" near the top of this document and `TS-STREAM-RECOVERY-01`'s ledger note for the actual, current state. The 72h PRAC soak remains genuinely not done and not fakeable by this session — that part of the original text below is still accurate.

<details>
<summary>Original text (superseded, kept for history)</summary>

**Not done, honestly:**
- **PR-F** (single priority scheduler replacing the 4 independent timers) — not implemented this session. This is a real architecture change to live execution-adjacent scheduling; implementing it blind in an audit pass, without the 72h soak this project's own process requires before trusting a reliability fix, would repeat exactly the mistake that caused the 08-31 recurrence. Recommend a dedicated session with its own soak plan.
- **PR-G / PR-H** (adaptive reconcile, persistence/health-latency tuning) — not started, not attempted.
- **72h PRAC soak** — cannot be run by this session. Requires the gateway running against real Topstep/ProjectX infrastructure, with credentials, for three continuous days, with someone watching for the failure modes documented in `docs/plans/2026-08-27-stream-recovery-implementation-plan.md` §4. This is real-world time this session does not have.

**What this session did instead:** confirmed precisely why the 08-31 recurrence happened (PR-F/G/H never started, soak gate never met — see 0.2/`TS-STREAM-RECOVERY-01`'s updated note), and added `TS-AUDIT31-PX-01` (circuit-breaker isolation) as one concrete, scoped piece of that work that's safe to implement in a future focused session.

</details>

---

## Fase 5 — PRAC-directed safety tests

**Blocked — cannot be executed by this session.** These require a live PRAC (practice) account, real credentials, and deliberate order placement against Topstep/ProjectX infrastructure — outside what an unattended audit pass should do. Documented here as the exact procedure for whoever runs it:

From `docs/PROJECTX-API-REFERENCE.md`'s own "NÃO CONFIRMADO" list, items 5/9/12/13 — still open after this audit, now cross-referenced from `ARCHITECTURE.md` and the two new ledger items:

1. Place a bracketed entry, then manually create a protective order through the account's native "Position Brackets" feature (not the API) — confirm whether `TS-AUDIT31-EX-01`'s `parentOrderId` fallback path actually attributes it, since there's no entry order to tag.
2. Trigger a partial close (`Position/partialCloseContract` or the gateway's own reduction path) and observe whether the surviving bracket rescales, stays fixed, or orphans.
3. Attempt to modify a leg while it's in `status: 8` (suspended, pre-fill) — behavior is currently undefined by both the API docs and this codebase.
4. Cancel one OCO leg manually and confirm whether the sibling cancels automatically or leaves the position with unilateral protection.

---

## What still needs a decision from you

1. **The 4 unmerged git worktrees** (`fix-pr129`, `fix-pr130`, `implement-ledger-1-27`, `prod-05-evidence-backpressure`) — keep, inspect-then-decide, or discard? I did not delete these.
2. **The 3 merged worktrees** (`acceptance-foundation`, `hermes-foundation`, `profile-foundation`) — I can clean these up properly (`git worktree remove`) on your go-ahead; didn't do it unprompted given the adjacent unmerged ones made me want a single explicit confirmation covering all 7 at once rather than guessing which subset you'd want touched.
3. **`.hermes-foundation2`/`.profile-implementation`/`.merge-hermes-work`** — these belong to the sibling profile repo's worktree registry or are a separate clone; cleaning them up means operating in that other repo, out of this audit's declared scope unless you want it extended there too.
4. **`TS-AUDIT31-EX-01`** (the real `status:8`-as-proven-protection bug) is the single highest-priority finding from this entire audit — recommend it's the next thing fixed, before any further reliability work, since it's a live-money correctness gap, not a stability nuisance.
