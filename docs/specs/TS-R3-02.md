# TS-R3-02 — Topstep session calendar and must-flat truth

**Issue:** [#65](https://github.com/GlitchTrader/glitch-topstep/issues/65)  
**Priority:** P1  
**Gateway version:** 0.1.3+

## Objective

Expose authoritative Topstep session boundaries and `must_flat_utc` in the decision packet so Hermes can plan `EXIT` before mandatory flat time, matching Glitch NT doctrine.

## Current state

- SOUL Topstep documents `must_flat_utc` as a stop line until packet truth exists.
- Gateway policy fields are operator-configured; no verified exchange/session calendar is published yet.

## Planned packet fields

```json
{
  "session": {
    "authority": "operator_configured | topstep_verified",
    "must_flat_utc": "2026-08-03T20:10:00.000Z | null",
    "entry_window_open": true,
    "notes": []
  }
}
```

## Acceptance

- session truth is explicit; null means unknown, not permission to invent;
- Hermes uses `must_flat_utc` as evidence for positioned `EXIT` planning, not as a cognition veto;
- operator reconciliation path documented when API lacks authoritative fields.

## Out of scope (this row)

- automatic session inference from bar gaps alone;
- hard deterministic flatten without operator enablement.
