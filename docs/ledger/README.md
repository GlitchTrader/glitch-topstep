`docs/ledger/ledger.json` is the canonical work authority for glitch-topstep: item selection, dependencies, ownership, and evidence live there under `TS-*` ids. `docs/ROADMAP.md` remains the durable roadmap reference (`roadmap_ref`) and expresses evidence-gated intent; it does not compete with the ledger for current work state. This ledger is separate from the NinjaTrader Glitch-Platform ledger.
