# TS-MULTI-01 / MULTI-02 / MULTI-03 — multi-instrument proof (2026-08-20)

Proof bundle for closing gateway issues #69, #124, #125 and Hermes GTHP-MULTI-01 (#108) scanner pairing.

## TS-MULTI-01 — second contract (MCL→MCLE)

| Criterion | Evidence |
|-----------|----------|
| Configurable without code fork | `resolveInstrumentUniverse(["MNQ","MES","MCL"], catalog)` |
| Outcomes scoped per instrument | Execution store + packet `contract.id` per session |
| Simulated armed session per contract | `tests/multi-contract-session.test.ts` — armed matrix for MNQ, MES, MCL/MCLE |
| One contract at a time | `simultaneous_exposure_enabled=false`; foreign exposure rejected |
| PARITY row | `docs/PARITY.md` instrument-general row updated |

Live armed MCL/MCLE trade outcome remains `unknown` in PARITY residual column; simulated acceptance is satisfied by sanitized fixtures.

## TS-MULTI-02 — rate-aware data plane

| Criterion | Evidence |
|-----------|----------|
| Allowlist → exact contract IDs + scope hash | `tests/instrument-universe.test.ts`, `tests/multi-instrument-data-plane.test.ts` |
| Hub supervisor restores subscriptions once | `tests/multi-instrument-reconnect.test.ts` |
| RateAwareScheduler headroom 4–6 instruments | `tests/scanner-packet.test.ts` scheduler fixture: peak 12 / budget 50 |
| Per-contract isolation | `MultiInstrumentMarketDataPlane` tests; stale instrument does not poison others |
| Observation-only until selected | `tests/fixtures/gateway/scanner_packet_observation_only.json` |

## TS-MULTI-03 — portfolio admission

| Criterion | Evidence |
|-----------|----------|
| Account-wide serialization lock | `tests/portfolio-admission.test.ts` |
| Protected downside includes foreign exposure | fail-closed when unpriceable or foreign without opt-in |
| Exact contract scope | `validatePortfolioSelection` + coordinator admission |
| `simultaneous_exposure_enabled` default false | config + tests |

## Hermes GTHP-MULTI-01

- Scanner packet fixture: `tests/fixtures/paired/multi01_scanner_packet.json` (gateway-sourced)
- Tests: `tests/test_paired_contracts.py` (`Multi01ScannerPacketFixtures`), existing `tests/test_scanner_contract.py`

## Regression commands

```powershell
cd C:\Users\arifr\Projects\glitch-topstep
npm run check -- tests/multi-contract-session.test.ts tests/multi-instrument-reconnect.test.ts tests/portfolio-admission.test.ts tests/scanner-packet.test.ts

cd C:\Users\arifr\Projects\glitch-topstep-hermes-profile
python -m unittest tests.test_paired_contracts.Multi01ScannerPacketFixtures tests.test_scanner_contract -v
```
