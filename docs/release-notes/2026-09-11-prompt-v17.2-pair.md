# Paired release: prompt v17.2

Gateway commit `928ed96` and profile commit `90d2ef6` use the same
`glitch-topstep-v17.2` prompt contract. The paired-contract SHA256 is
`83992E681F3EA0784667F36DAE509E1F04E96318CC61FED5B0E58F5B22D8897A`.

This release preserves Hermes subprocess diagnostics in sanitized evidence,
serializes skill preload, and adds replay-only MNQ/MES/MCL selection fixtures.
The runtime remains `shadow-only`; no overnight, canary, armed paper, sizing,
or live orders are authorized. Provider/model failures still require
independent reauditing before any promotion.
