{
  "name": "glitch-topstep",
  "version": "0.1.0",
  "private": true,
  "description": "Local Glitch Topstep gateway (shadow) for Hermes glitch-topstep profile",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "auth:test": "node scripts/test-auth.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@microsoft/signalr": "^8.0.17",
    "dotenv": "^16.4.7"
  }
}
