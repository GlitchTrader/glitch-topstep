# Archived operator scripts

One-off PRAC directed tests and watch helpers. Nothing here is called by CI or `npm run check`.
Soak checklist / sample / capture / flatten stay in `scripts/`. Git history keeps the original paths.
